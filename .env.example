# Server-side AI credentials. Never expose these to the browser.
# Lovable AI Gateway (used by default; managed automatically in Lovable projects)
LOVABLE_API_KEY=your_api_key_here

# If you swap the provider in src/lib/pulse-analysis.server.ts, set its key here instead:
# GEMINI_API_KEY=your_api_key_here
